import 'package:flutter/material.dart';

import '../../../../core/widgets/more/app_icon_selector.dart';

/// Reusable icon selector grid for habit creation flow.
class IconSelector extends StatelessWidget {
  /// Creates an icon selector widget.
  const IconSelector({super.key});

  @override
  Widget build(BuildContext context) {
    return const AppIconSelector();
  }
}
