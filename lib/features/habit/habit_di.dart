/// Dependency registration placeholder for habit feature.
final class HabitDi {
  /// Private constructor prevents accidental instantiation.
  const HabitDi._();

  /// Registers habit feature dependencies in future integration steps.
  static Future<void> register() async {}
}
