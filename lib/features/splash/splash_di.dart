/// Dependency registration placeholder for splash feature.
final class SplashDi {
  /// Private constructor prevents accidental instantiation.
  const SplashDi._();

  /// Registers splash feature dependencies in future integration steps.
  static Future<void> register() async {}
}
